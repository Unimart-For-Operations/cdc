---
repo: meta
date: 2026-08-19
commit: 2fe7695
type: refactor
scope: builder
tags: [commit-log]
---

# refactor(builder): adapt sandbox build for repo-root context

The sandbox Dockerfile now imports cmdr modules at build time, so
the build context must be the repo root (not just containers/sandbox).
The nvim-astro config sync is no longer needed — it arrives through
the same home-manager activation that delivers all other configs.
